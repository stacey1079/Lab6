@model IEnumerable<EnrollmentApplication.Models.Student>

<div id="searchresults">
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.LastName</li>
        }
    </ul>
</div>
